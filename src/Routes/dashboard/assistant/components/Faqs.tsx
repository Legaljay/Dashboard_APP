import Common from "./memory-tabs-common";
import FaqTemplate from "./memory-tabs-common/memory-modals/FaqTemplate";

const Faqs: React.FC = () => {
    return <Common tab="faqs" template={FaqTemplate}/>;
};

export default Faqs;